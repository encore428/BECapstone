class Item {
  constructor({ id, title, completed, tid, uid }) {
    this.id = id
    this.title = title
    this.completed = completed
    this.tid = tid
    this.uid = uid
  }
}

module.exports = Item